<div class="addons-wrap d-flex flex-column overflow-y-auto">
    <div class="status d-flex flex-justify-between p-2">
        <div class="title p-1">
            <i class="fa fa-book"></i> {{ site.title }}
        </div>
        <div class="branch p-1">
            <span class="name">
               
            </span>
            <i class="fa fa-caret-down"></i>
        </div>
    </div>
    <div class="addons d-flex flex-column height-full p-2 d-none">
        <dl>
            <dt>{{ site.title | default: "test" }}</dt>
            <dd>
                <a href="{{ site.url }}" title="Homepage">
                    <i class="fa fa-android"></i>
                    Homepage
                </a>
            </dd>
            <dd>
                <a href="{{ site.url }}" title="Issues">
                    <i class="fa fa-question-circle-o"></i>
                    Issues
                </a>
            </dd>
            <dd>
                <a href="{{ site.url }}" title="Size Kb">
                    <i class="fa fa-download"></i>
                    Download
                </a>
            </dd>
        </dl>
        <hr>
        <div class="license f6 pb-2">
            This
            <a href="{{ site.url }}/" title="{{ site.title }}">website</a>
            was created by
            <a href="{{ site.urll }}">eugene</a>.
        </div>
    </div>
</div>
